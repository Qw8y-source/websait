# ─────────────────────────────────────────────────────────────
# SmakCity Vercel Proxy — .env.example
# Скопіюй як .env і заповни реальними значеннями:
#   cp .env.example .env
# .env вже є в .gitignore — він НЕ потрапить у Git.
# ─────────────────────────────────────────────────────────────

TG_BOT_TOKEN=ВАШ_ТОКЕН_ВІД_BOTFATHER
TG_CHAT_ID=ВАШ_CHAT_ID
