/* ============================================================
   SMAKCITY — ШАБЛОН КОНФІГУРАЦІЇ
   ────────────────────────────────────────────────────────────
   1. Скопіюй цей файл: cp config.example.js config.js
   2. Заповни значення нижче своїми даними
   3. config.js вже є в .gitignore — він НЕ потрапить у Git
   ────────────────────────────────────────────────────────────
   🔒 РІВНІ БЕЗПЕКИ:
   
   Варіант A (простий) — Пряма відправка в Telegram:
     Заповни BOT_TOKEN і CHAT_ID нижче.
     ⚠️  Токен буде видний у DevTools браузера!
         Підходить лише для демо або якщо бот не критичний.
   
   Варіант B (безпечний) — Через Serverless-проксі:
     Залиш BOT_TOKEN / CHAT_ID порожніми.
     Заповни PROXY_URL адресою свого Worker/Function.
     Токен зберігається тільки на сервері (Cloudflare / Vercel).
   ============================================================ */

const SMAKCITY_CONFIG = {

  // ── Варіант A: Пряма відправка ──────────────────────────────
  BOT_TOKEN: "",   // 🔑 Вставте токен від @BotFather
  CHAT_ID:   "",   // 💬 ID чату (отримай через @userinfobot)

  // ── Варіант B: Проксі (рекомендовано для production) ────────
  // Якщо вказано PROXY_URL — BOT_TOKEN і CHAT_ID ігноруються.
  // Деплой Worker/Function дивись у папці /proxy/
  PROXY_URL: "",   // Приклад: "https://smakcity.YOUR_SUBDOMAIN.workers.dev/send"
                   // Або:     "https://your-app.vercel.app/api/send-order"

  // ── Демо-режим ───────────────────────────────────────────────
  // true → не відправляти в Telegram, лише імітувати успіх
  DEMO_MODE: false,

};
